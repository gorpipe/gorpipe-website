update_pages_branch:
  stage: deploy
  script: 
    - git checkout gl-pages
    - git merge master --no-edit
  only:
    - master

pages:
  stage: deploy
  image: node:14.5
  script:
    - npm run build
    - mv out public
  artifacts:
    paths:
      - public
  only:
    - gl-pages
